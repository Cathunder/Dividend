spring:
  application:
    name: Dividend

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    defer-datasource-initialization: true
#      use-new-id-generator-mappings: false (Spring Boot 3.x부터 제거되어서 삭제)

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:dayone;DB_CLOSE_DELAY=-1
    username: sa
    password:

scheduler:
  scrap:
    yahoo: "0 0 0 * * *"